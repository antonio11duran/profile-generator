const Engineer = require("../lib/Engineer");

describe('getRole', () => {
    it('should return role of employee', () => {
        expect('Engineer');
    })
});

describe('getGithub', () => {
    it('should return github of employee', () => {
        expect(this.github);
    })
});