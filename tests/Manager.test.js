const Manager = require("../lib/Manager");

describe('getRole', () => {
    it('should return role of employee', () => {
        expect('Manager');
    })
});

describe('getOfficeNumber', () => {
    it('should return office number of employee', () => {
        expect(this.officeNumber);
    })
});