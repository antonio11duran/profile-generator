const Intern = require("../lib/Intern");

describe('getRole', () => {
    it('should return role of employee', () => {
        expect('Intern');
    })
});

describe('getGithub', () => {
    it('should return school of employee', () => {
        expect(this.school);
    })
});